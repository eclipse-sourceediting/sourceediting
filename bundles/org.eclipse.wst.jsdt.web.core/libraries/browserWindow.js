/**
  * Object Window()
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var navigator= new Navigator();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var document= new HTMLDocument();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var length="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var parent= new Window();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var top= new Window();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var scrollbars= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var name="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var scrollX="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var scrollY="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function scrollTo(arg1,arg2){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function scrollBy(arg1,arg2){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function getSelection(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function scrollByLines(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function scrollByPages(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function sizeToContent(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function dump(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function setTimeout(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function setInterval(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function clearTimeout(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function clearInterval(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function setResizable(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function captureEvents(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function releaseEventsarg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function routeEvent(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function enableExternalCapture(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function disableExternalCapture(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function prompt(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function open(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function openDialog(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var frames= new Window();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function find(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var self= new Window();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var screen= new Screen();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var history= new Array();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var content= new Window();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var menubar= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var toolbar= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var locationbar= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var personalbar= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var statusbar= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var directories= new BarProp();
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var closed="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var opener="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var status="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var defaultStatus="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var innerWidth=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var innerHeight=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var outerWidth=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var outerHeight=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var screenX=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var screenY=0;
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var pageXOffset="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var pageYOffset="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var scrollMaxX="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var scrollMaxY="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var fullScreen="";
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function alert(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function confirm(arg1){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function focus(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function blur(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function back(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function forward(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function home(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function stop(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function print(){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function moveTo(arg1,arg2){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function moveBy(arg1,arg2){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function resizeTo(arg1,arg2){};
/**

  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function resizeBy(arg1,arg2){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function scroll(arg1,arg2){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function close(){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function updateCommands(arg1){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function atob(arg1){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function btoa(arg1){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var frameElement="";
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function removeEventListener(arg1,arg2,arg3){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function dispatchEvent(arg1){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
function getComputedStyle(arg1,arg2){};
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var sessionStorage="";
/**
  * @see Window
  * @since Non Standard, but general usage among popular browsers.
 */
var globalStorage= new StorageList();